create table notifications (id uuid default uuid_generate_v4() primary key, created_at timestamp with time zone default timezone("utc" :: text, now()) not null, user_id uuid references auth.users(id), title text not null, description text, is_read boolean default false, related_type text, related_id uuid);
